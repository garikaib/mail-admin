symbols {
  "LOCAL_BL_FROM" {
    weight = 1000.0;
    description = "Sender is locally blacklisted";
    groups = ["local_bl"];
  }
  "AUTO_PHISH_DOMAIN_BLOCK" {
    weight = 1000.0;
    description = "Sender domain was auto-blacklisted by phishing rules";
    groups = ["local_bl"];
  }
  "LOCAL_WL_DOMAIN" {
    weight = -100.0;
    description = "Sender domain is whitelisted";
    groups = ["local_wl"];
  }
}
